# 黑天牛 YOLO 模型效果说明

## 使用的模型

- 最终权重：`weights/black_longhorn_best.pt`（与本次提供的 `best.pt` SHA-256 一致）
- 模型：YOLO11s
- 识别类别：1 类，`longhorn_beetle`（网页显示为“黑天牛”）
- 训练输入尺寸：640
- 模型内记录的最佳验证结果见下表
- 独立摄像头测试图片：45 张

## 验证集指标

本次权重内保存的验证指标为：

| 指标 | 结果 | 说明 |
|---|---:|---|
| Precision | 67.8% | 模型画出的框中，约 67.8% 与验证集标注匹配 |
| Recall | 60.3% | 验证集已标注目标中，约 60.3% 被模型找到 |
| mAP50 | 60.0% | IoU=0.5 条件下的综合检测表现 |
| mAP50-95 | 24.1% | 严格定位指标较低，现场必须保留复核流程 |

验证集来自原始标注数据的拆分，样本量较小，因此这些数值可能偏乐观，不能直接代表现场长期效果。

## 摄像头实拍测试结果

使用默认置信度阈值 `0.25` 对 45 张摄像头照片进行了批量检测：

- 检测到候选：12 张
- 未检测到候选：33 张
- 候选图片占比：26.7%
- 候选框总数：15 个
- 最高单帧置信度：60.3%

这里的 26.7% 是“有候选框照片占比”，不是模型准确率。摄像头测试集尚未逐张人工标注真值，候选框可能是正确目标，也可能是误报。不能用这组数据宣称零误诊或计算现场准确率。

要计算真正的现场误报率和漏检率，仍需给这 45 张图片补充人工真值标注，并另建不含虫子的负样本测试集。

## 网页使用

1. 正常重启项目服务。
2. 打开 `http://localhost:9117/jhds/ai-learn`。
3. 点击“查看测试集”，可查看统计、带框照片，并筛选“已识别/未识别”。
4. 点击“上传图片识别”，可上传现场图片并查看目标数量和每个框的置信度。
5. 置信度默认是 `0.25`。阈值降低会减少漏检但增加误报；阈值升高会减少误报但增加漏检。现场建议先使用 `0.25`，人工复核一段时间后再调整。

## 实时视频监测

AI 轨道巡检页面已接入本次权重并默认启用“虫害监测”：

- 浏览器每秒从正在播放的视频截取一帧，不会重复连接摄像头。
- 后台常驻加载 YOLO 和 GPU；首次加载约 7 秒，预热后单帧约 0.22 秒。
- 橙色框表示单帧候选，红色框才表示通过多帧复核的目标。
- 默认要求同一位置在最近 5 个采样帧内出现至少 3 次，框间 IoU 不低于 0.25，多帧平均置信度不低于 0.35，且至少一帧置信度达到 0.50。
- 只有多帧确认后才显示红色警告、尝试播放提示音，并在报警中心写入“紧急”告警。
- 同一持续事件默认每 60 秒最多写一条告警，避免数据库被每秒重复记录淹没。
- 页面上的“虫害监测 开/关”可以临时停止或恢复识别。

可通过 `AI_YOLO_VERIFY_*` 系列变量调整多帧规则，通过 `AI_YOLO_REALTIME_INTERVAL_MS` 调整采样间隔，通过 `AI_YOLO_ALARM_COOLDOWN_SECONDS` 调整告警去重时间，通过 `AI_YOLO_ALARM_ENABLED=false` 停止写报警中心但保留画框和页面警告。多帧验证能显著过滤瞬时误报，但不能从统计上保证零误诊；高风险处置仍须人工查看告警截图。

## 本机依赖

- Python：`D:/jhdss-tools/yolo-venv311/Scripts/python.exe`
- 推理脚本：`scripts/yolo_infer.py`
- 权重和目录均可通过 `application.yml` 中的 `ai.yolo` 配置或对应环境变量覆盖。
